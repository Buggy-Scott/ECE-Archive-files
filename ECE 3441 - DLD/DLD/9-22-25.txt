Lecture note 9
Functions
    Preprocessor 
    Which is #include <stdio.h>
    this allows us to start from c source 
    code to the preprocessor to the complier.

Not that 0is is traditionally starts 
in column 1 buut can be places lower if needed.
But it must occur before you start any code.

-----Preprocessor directives
Directive Meaning
    #include Include the contents of a text file.
    #define Define a macro.
    #undef Cancel a previous #define.
    #if If a test succeeds, take specified action.
    #ifdef If a macro is defined, take specified actions.
    #ifndef Opposite of #ifdef--if a macro is not defined, take specified actions.
    #else If the previous #if, #ifdef, or #ifndef fails, take specified actions.
    #endif Mark the end of an #if, #ifdef, or #ifndef body.
    #elif “Else if”--a way around nested #if-#else constructs.
    #line Set line number for the compiler to use when issuing warning or
    error messages.
    #error Specify a compile-time error and an accompanying message.
    #pragma Provide implementation-specific information to the compiler.
    # Ignore this line.


file inclusion can be used with a
 preprocessor of #include "myfile.h"   
  ^ which allows thee file to be found  
 in the working directory.

 Subheading -----System Header File Purpose
    ctype.h Functions for testing and modifying characters (sec. 9.5).
    float.h Describes local floating-point conditions (Section 3.2 ).
    limits.h Describes local integer conditions ( section 3.1).
    math.h Math functions (section 2.6).
    setjmp.h Nonlocal jumps ( section 11.6)
    signal.h Exception handling (section 11.6);
    stdarg.h Functions with an arbitrary number of arguments (sec 5.10).
    stdio.h input/output
    stdlib.h General utilities ( section 5.1 ).
    string.h String-handling functions ( section 6.6 ).
    time.h Time functions ( section 7.9).


Macro Example----------------
#define EOF(-1)

#define Normal Meetings= 3;
#define Special Meetings= 2;
#define TImePerMeetinf= 20;

# define Total Meetings = NormalMeetings + SPecialMeetings;
 This macro of  totmeetings will expand ti 3+5 =5.
 This is genearlly not ideal, 
 because Totalmeeting is kind of consider as a variable.

But something like #define TotalTime = TotalMeetings * TimePerMeeting; 
is equvalent to 
#defeine TotalTime = NormalMeetigs + SpecialMeetings * TimePerMeeting;
This will expand into 3+2*20 = 43 instead of messing up and equaling to 100.

Note that you can't overridde a #define function with anothr defeine function.
you will have to use the #undef 
function to undefine the oriiginal define function before redefeining it.
Example:
#define TRUE 1
#define TRUE 2 
This is illegal 
but
#define TRUE 1
#undef TRUE 
#define TRUE 2
this is legael.
----------------------------------------------------
Parameterized MArcros compareds to Functions
    No arguments are passeda to parameterized macro( min).
    parameterized macros returns no values.
    preprocessor merely replaces the macros with the definition.
        So min( num1, num2);
        by 
        (((num1 )< (num2))? (num1):(num2));
    ^ this is use to be more efficient that a function.

Function calls
    will have an assoicated overhead.
    works in a system that must copy 
    any arhuments that pass through the function, 
    which will track of where to resume the program 
    when the function returns.
    No type of checking is needed in 0a macros.

    It have an advantage of using the min function to find
     the minimum of any two numericasl data type.
     But alsoa has the disadvantage of introducing errors that can be difficult to uncover.
     like producing presumable useless values 
     when the argument is a string and the other is a float.


------------------------------ Misc. Notes
#line <- will specifies line number by complier.

The #pragma directives offer a way for each compiler to offer machine- and
operating-system-specific features while retaining overall compatibility with the C and
C++ languages. Pragmas are machine- or operating-system-specific by definition, and
are usually different for every compiler.
#pragma token-string
inline- insert a copy of the function when it is called
